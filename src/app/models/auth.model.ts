export interface UserDetails {
    username?: string;
    firstName?: string;
    lastName?: string;
    authority?: number;
}

export interface AuthDetails {
    username?: string;
    password?: string;
}